# Part 2 – Model Improvement Notebooks

   - `vehicle_detection_pipeline.ipynb` — Full training + evaluation pipeline. Run in Google Colab with a GPU runtime (T4). Trains the improved model from scratch with all improvement strategies applied.
   - `vehicle_detection_eval_only.ipynb` — Evaluation only. Loads model weights from HuggingFace. No GPU or training needed — use this to verify results.

   Random seed: 42 (fixed at top of each notebook).
